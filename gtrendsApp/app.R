library(shiny)
library(dplyr)
library(shinydashboard)
#library(policyPlot)
library(plotly)
#library(profvis)
# Define UI for miles per gallon app ----
#blah
#profvis({
#runApp(shinyApp(ui, server))
#})

shinyApp(ui, server)
